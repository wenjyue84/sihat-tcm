/**
 * TCM Data
 * 
 * Centralized exports for TCM static data files.
 * These are pure data (nouns), not utilities (verbs).
 */

// Re-export from existing locations for backward compatibility
export * from "./solar-terms-data";
export * from "./daily-tips-data";
