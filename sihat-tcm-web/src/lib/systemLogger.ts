/**
 * System Logger - Legacy Re-export
 *
 * @deprecated This file is maintained for backward compatibility.
 * Use the organized logging utilities from './logging' instead:
 *
 * import { log, logInfo, logError } from '@/lib/logging';
 *
 * For client components, use: import { devLog } from '@/lib/logging';
 */

// Re-export from organized logging for backward compatibility
export * from "./logging/system-logger";
